---
title: "Create Content"
api: "POST /v1/content"
description: "Creates a new content item. Videos are created with status `waiting_for_upload` — use the Uploads API to upload the video file. Carousels are created with status `ready` immediately."
---

## Request body

<ParamField body="contentType" type="string" default="video">
  `"video"` or `"carousel"`.
</ParamField>

<ParamField body="title" type="string" required>
  Display title. Max 200 characters.
</ParamField>

<ParamField body="description" type="string">
  Optional description. Max 2000 characters.
</ParamField>

<ParamField body="tags" type="string[]">
  Tags for categorization and filtering.
</ParamField>

<ParamField body="section" type="string">
  Editorial section (e.g., `"Sports"`, `"News"`). Max 200 characters.
</ParamField>

<ParamField body="author" type="string">
  Content author name. Max 200 characters.
</ParamField>

<ParamField body="caption" type="string">
  Carousel caption text. Max 500 characters. Only applies to carousels.
</ParamField>

<ParamField body="autoScrollInterval" type="number">
  Seconds between automatic image transitions. Only applies to carousels.
</ParamField>

<ParamField body="pinPosition" type="integer">
  Pin this content to a fixed feed position (0-indexed).
</ParamField>

<ParamField body="publishAt" type="string">
  ISO 8601 timestamp. Content won't appear in the feed before this time.
</ParamField>

<ParamField body="expiresAt" type="string">
  ISO 8601 timestamp. Content is removed from the feed after this time.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://api.shortkit.dev/v1/content \
  -H "Authorization: Bearer sk_live_your_secret_key" \
  -H "Content-Type: application/json" \
  -d '{
    "contentType": "carousel",
    "title": "Top 5 Weekend Destinations",
    "section": "Travel",
    "author": "Travel Desk",
    "tags": ["travel", "weekend"],
    "caption": "Swipe through our picks for this weekend.",
    "autoScrollInterval": 5.0
  }'
```

```python Python
import requests

resp = requests.post(
    "https://api.shortkit.dev/v1/content",
    headers={
        "Authorization": "Bearer sk_live_your_secret_key",
        "Content-Type": "application/json",
    },
    json={
        "contentType": "carousel",
        "title": "Top 5 Weekend Destinations",
        "section": "Travel",
        "author": "Travel Desk",
        "tags": ["travel", "weekend"],
        "caption": "Swipe through our picks for this weekend.",
        "autoScrollInterval": 5.0,
    },
)
content = resp.json()["data"]
```

```javascript Node.js
const resp = await fetch("https://api.shortkit.dev/v1/content", {
  method: "POST",
  headers: {
    Authorization: "Bearer sk_live_your_secret_key",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    contentType: "carousel",
    title: "Top 5 Weekend Destinations",
    section: "Travel",
    author: "Travel Desk",
    tags: ["travel", "weekend"],
    caption: "Swipe through our picks for this weekend.",
    autoScrollInterval: 5.0,
  }),
});
const { data: content } = await resp.json();
```
</RequestExample>

<ResponseExample>
```json 201
{
  "data": {
    "id": "d290f1ee-6c54-4b01-90e6-d701748f0851",
    "contentType": "carousel",
    "title": "Top 5 Weekend Destinations",
    "description": null,
    "status": "ready",
    "duration": null,
    "streamingUrl": null,
    "thumbnailUrl": null,
    "section": "Travel",
    "author": "Travel Desk",
    "tags": ["travel", "weekend"],
    "customMetadata": {},
    "captionTracks": [],
    "publicationUrls": [],
    "images": [],
    "carouselCaption": "Swipe through our picks for this weekend.",
    "autoScrollInterval": 5.0,
    "pinPosition": null,
    "isSuppressed": false,
    "publishAt": null,
    "expiresAt": null,
    "views": 0,
    "createdAt": "2026-02-05T12:00:00Z",
    "updatedAt": "2026-02-05T12:00:00Z"
  },
  "meta": {
    "request_id": "req_abc123def456"
  }
}
```
</ResponseExample>
