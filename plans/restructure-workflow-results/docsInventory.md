# Docs site inventory — `/Users/michaelseleman/orca/workspaces/shortkit-docs-repo/local-fable-docs`

## Docs tab (13 nav pages + 1 orphan)

| Path | Documents | Platform coverage | Size | Smells |
|---|---|---|---|---|
| `sdk/installation.mdx` | Requirements table; package structure; install steps (SPM/Gradle/pub/npm/Expo plugin/CDN); verify snippets (`ShortKit`, `ShortKitFeedView(Controller)`, `ShortKitFeedFragment`, `ShortKitProvider`/`ShortKitFeed`, `sk.createFeed`); 12-item troubleshooting accordion | All real: iOS, Android, Flutter, RN, Expo, Web | 540 ln | Install steps duplicated (abridged) in quickstart; pinned versions `0.2.22`/web `0.3.0` are stale vs `VERSIONS.md` (0.2.55) |
| `quickstart.mdx` | API key, condensed install, display-feed snippets per platform, "Adding your own content" (portal/import/upload), next steps | All real (incl. Expo, Web) | 282 ln | Repeats installation content; acceptable as intentional |
| `guides/content-types.mdx` | `FeedItem` enum; `ContentItem` (+`CaptionTrack`, `CaptionSource`, `customMetadata`/`JSONValue`); `FeedInput.video`/`ContentOrigin`; `VideoCarouselVideoInput`; fallback URLs; `ImageCarouselItem`, `VideoCarouselItem`, `SurveyItem`, `AdSlot`; custom-UI switch examples; per-type data matrix | Swift/Kotlin/RN/Web tabs real; **Flutter has NO tabs anywhere on this page**; Web carousel/survey marked "rendering deferred — coming soon" | 868 ln | Oversized; mixes model reference + custom-feed-input guide + fallback runtime behavior (player concern); `ContentItem` table duplicated in `sdk/overlays` and `api/content/object` |
| `guides/upload-pipeline.mdx` | Video upload flow (`POST /v1/content/upload`, PUT to signed URL, poll `GET /v1/content/{id}`, `callbackUrl` webhooks); `origin: ios_upload`; carousel creation (`POST /v1/content`), image upload/reorder; PATCH metadata (shallow-merge); captions upload; publication URLs; archive | REST-only (cURL/Python/Node) — platform-agnostic, fine | 539 ln | Documents 7+ endpoints that have **no API Reference page** (see below); request field tables duplicate `api/content/upload`; `status` vs `uploadStatus` quirk note |
| `guides/live-streams.mdx` | Lifecycle walkthrough; protocol choice (`hls`/`webrtc`); create/broadcast (ffmpeg, OBS split); SDK live fields (`isLive`, `liveStreamStatus`, `liveStreamId`, `currentViewers`, `startedAt`); heartbeat; custom-feed playbackId; clips; end | Swift/Kotlin/RN snippets; no Flutter or Web tabs (unflagged) | 239 ln | Healthy guide; minor overlap with `api/live-streams/*` examples |
| `guides/language-detection.mdx` | `customMetadata.language` (BCP 47), `und`, code table, `FeedFilter(metadata:)` filtering | All 5 real | 151 ln | Filter-by-language examples duplicated near-verbatim in `sdk/configuration#filtering-by-language` |
| `sdk/feed.mdx` | `ShortKitFeedViewController` (UIKit), `ShortKitFeedView` (SwiftUI), `ShortKitFeedFragment`, embedding patterns, lifecycle per platform, filtered feeds, partial-height | All 5 real | 292 ln | **Missing `description` frontmatter**; iOS-centric headings (`ShortKitFeedViewController` H2s) with other platforms shoehorned into tabs; Android imports inconsistent (`com.shortkit.sdk.*` in installation/quickstart vs `dev.shortkit.*` here) |
| `sdk/player-and-widget.mdx` | `fetchContent(limit:filter:)`; single player: `PlayerConfig`, `ShortKitPlayerViewController/View`, `activate()/deactivate()`, RN `active` prop; widget: `WidgetConfig`, `WidgetLayout` (.carousel/.grid), `WidgetPlaybackMode` (incl. `gridAlternating`), `WidgetInput`, widget props/callbacks (`onCardTap`, `onContentHeightChange`, `setFeedItems`), embedded grid + `appendItems`, auto-rotation, click actions | Player: iOS/Android/RN/Web real. Widget advanced (layout, playbackMode, WidgetInput, properties, grids): **iOS only — Android & RN "Coming soon" ×5**. Flutter: entire page "coming soon" | 799 ln | Oversized; two components on one page; heavy stub density; title in nav group "Display" vs `sdk/player` in "Build Your Experience" confuses player vs Player |
| `sdk/configuration.mdx` | `ShortKit` initializer params (incl. `customDimensions`, `feedItems`); `FeedConfig` (all overlay modes, `feedHeight`, `scrollAxis`, `feedSource`); `FeedFilter`; language filtering; complete example | All 5 real | 756 ln | "Filtering by language" section duplicates both the FeedFilter examples directly above it AND `guides/language-detection` |
| `sdk/player.mdx` | `ShortKitPlayer`: 15 publishers (`time`, `playerState`, `currentItem`, `didLoop`, `feedTransition`, `formatChange`, `activeCue`, `feedScrollPhase`, `feedReady`…); sync accessors; 13 commands (`play`…`seekThumbnail`); `PlayerState` machine; event types (`PlayerTime`, `LoopEvent`, `FeedTransitionEvent`, `FormatChangeEvent`, `ContentSignal`); minimal-overlay example | All 5 real | 761 ln | Publisher + command tables duplicated in `sdk/overlays`; "custom overlay" example overlaps overlays page |
| `sdk/overlays.mdx` | `VideoOverlayMode` (.custom/.swiftUI/.none); `FeedOverlay` protocol; Flutter `ShortKitOverlayEngine`/`OverlayState`/`ShortKitOverlayCommands`; SwiftUI `CellContent`; full `ShortKitPlayer` re-reference; `ContentItem` table; scrubber + captions examples; engagement signals; `CarouselOverlay`, `VideoCarouselOverlay`, `SurveyOverlay`, `AdOverlay` protocols; `NativeAdContent` | Video overlays: all 5. Carousel/survey/ad protocols: Swift/Kotlin only; Flutter "video overlays only, carousel coming soon"; RN survey = template only, no `adOverlay` | 1307 ln | **Largest page; clear split candidate** (5 protocols + 2 long tutorials + duplicated player reference). Internal inconsistency: RN config shown as `overlay: {type:"custom"}` in one snippet and `videoOverlay: {mode:"custom"}` in another (line ~221) |
| `sdk/carousel.mdx` | Video carousel runtime: default behavior, `shortKit.carousel` (`next/previous/setActiveIndex`, `activeIndex`/`activeCarouselChanged` publishers, sync accessors), RN `ShortKitCommands.carousel*` + `useShortKitCarousel()`, `onVideoCarouselCellTap`/`LongPress` payloads, `CarouselCompletionEvent`, recipes | **iOS + RN only** — Android/Flutter/Web absent and not flagged | 450 ln | Platform gap unacknowledged; some `initialPageIndex` behavior duplicated with content-types |
| `sdk/identity.mdx` | Anonymous UUID, `setUserId`, init-time `userId`, `clearUserId`, `POST /v1/identity/resolve` mechanics, `X-User-Id` header | All 5 real | 190 ln | "User identity" section in `sdk/analytics` duplicates setUserId/clearUserId with full 5-tab snippets |
| `sdk/analytics.mdx` | Auto-collected metrics tables (engagement, QoE, fallback, feed session, ads, context); `customDimensions` (custom_1–5); `sendContentSignal`; identity recap; pointers to metrics API | All 5 real | 287 ln | **Empty `description: ""` frontmatter**; duplicates identity + content-signal content (3rd place signals appear) |
| `coming-soon.mdx` ("Roadmap") | Two roadmap cards | — | 18 ln | **Orphan — not in docs.json nav** |

## API Reference tab

- **Overview**: `overview`, `authentication`, `errors` — complete.
- **Content** (9): object, upload, import, list, get, count, search, update, delete. **Missing pages for endpoints documented only in the upload-pipeline guide:** `POST /v1/content` (carousel create), `POST /v1/content/{id}/images`, `PUT .../images/order`, `POST .../captions`, publication-urls CRUD (POST/PUT/PUT order/DELETE), and `GET /v1/feed` (referenced but never documented). Webhook/callback events live only as a section inside `upload.mdx`.
- **Live Streams** (8): object, create, list, get, status, end, delete, clip — complete vs guide; no update/PATCH page (may not exist).
- **Surveys** (7): object, create, list, get, update, delete, submit-response — complete.
- **Analytics & Events** (5): event-object, list-events, metrics, get-metric, get-timeseries. `sdk/analytics` promises "dimension breakdowns … and raw data export" — **no breakdown or export endpoint page exists**.
- **Ad Config** (3): object, get, update — complete.
- **Missing entirely:** `POST /v1/identity/resolve` (referenced in `sdk/identity`); no webhooks/callbacks reference page.

## Cross-page overlap/duplication pairs

1. `sdk/player.mdx` ↔ `sdk/overlays.mdx` — publisher table, command table, and overlay example duplicated wholesale.
2. `sdk/overlays.mdx` ↔ `guides/content-types.mdx` ↔ `api/content/object.mdx` — three `ContentItem` field tables.
3. `sdk/configuration.mdx` ↔ `guides/language-detection.mdx` — language-filter examples duplicated 5-platform-tabs verbatim (plus internal duplication within configuration itself).
4. `sdk/identity.mdx` ↔ `sdk/analytics.mdx` — setUserId/clearUserId full snippet sets in both.
5. `sdk/analytics.mdx` ↔ `sdk/overlays.mdx` ↔ `sdk/player.mdx` — `sendContentSignal` documented in three places.
6. `sdk/installation.mdx` ↔ `quickstart.mdx` — install + verify snippets.
7. `guides/upload-pipeline.mdx` ↔ `api/content/upload.mdx` — request fields and callback events.
8. `guides/live-streams.mdx` ↔ `api/live-streams/{create,end,clip}.mdx` — request/response examples.
9. `sdk/carousel.mdx` ↔ `guides/content-types.mdx` — `initialPageIndex` semantics described in both.
10. `sdk/player.mdx` ("Player") ↔ `sdk/player-and-widget.mdx` ("Widgets & Entry Points") — not true duplication but a confusing naming pair: the former is the playback-control interface, the latter the embeddable player component; both keyword "player".