---
title: "Import from Instagram"
description: "Import Reels from Instagram. Accepts up to 20 `urls` \u2014 individual Reel links or profile links, which are expanded up to `maxReelsPerProfile` (default 500). Returns a `jobId` with counts of Reels queued and skipped (previously imported Reels are deduplicated); processing is asynchronous, with optional progress delivery to `callbackUrl`."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Import Reels from Instagram. Accepts up to 20 `urls` — individual Reel
          links or profile links, which are expanded up to `maxReelsPerProfile`
          (default 500). Returns a `jobId` with counts of Reels queued and
          skipped (previously imported Reels are deduplicated); processing is
          asynchronous, with optional progress delivery to `callbackUrl`.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/content/import/instagram","method":"post"}]} />
    </>
  );
}