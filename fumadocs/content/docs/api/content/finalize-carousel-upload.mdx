---
title: "Finalize carousel upload"
description: "Finalize a carousel created via the upload endpoint after sending the image files to their signed upload URLs. Each image slot is validated \u2014 the file must exist, be an image, and be 10 MB or less \u2014 and the carousel becomes `ready` if at least one image is valid, otherwise `errored`. Returns per-image results; only applies to carousels still awaiting upload."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Finalize a carousel created via the upload endpoint after sending the
          image files to their signed upload URLs. Each image slot is validated
          — the file must exist, be an image, and be 10 MB or less — and the
          carousel becomes `ready` if at least one image is valid, otherwise
          `errored`. Returns per-image results; only applies to carousels still
          awaiting upload.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/content/{content_id}/finalize","method":"post"}]} />
    </>
  );
}