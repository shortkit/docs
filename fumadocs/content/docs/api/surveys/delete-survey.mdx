---
title: Delete survey
description: >-
  Archive a survey. This sets its status to `archived` and removes it from the
  feed immediately, but preserves all response data — archived surveys can still
  be retrieved and listed.
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: DELETE
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Archive a survey. This sets its status to `archived` and removes it
          from the feed immediately, but preserves all response data — archived
          surveys can still be retrieved and listed.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/surveys/{survey_id}","method":"delete"}]} />
    </>
  );
}