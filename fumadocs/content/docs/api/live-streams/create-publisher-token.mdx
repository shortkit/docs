---
title: Create publisher token
description: >-
  Issue a short-lived publisher token (about 4 hours) for broadcasting to a
  WebRTC live stream, along with the `wsUrl` and WHIP ingest details. Only
  available for streams created with `protocol: "webrtc"` — returns 400 for HLS
  streams. Requires your secret key, so call it from your backend and hand the
  token to the broadcasting client.
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Issue a short-lived publisher token (about 4 hours) for broadcasting
          to a WebRTC live stream, along with the `wsUrl` and WHIP ingest
          details. Only available for streams created with `protocol: "webrtc"`
          — returns 400 for HLS streams. Requires your secret key, so call it
          from your backend and hand the token to the broadcasting client.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/live-streams/{stream_id}/publisher-token","method":"get"}]} />
    </>
  );
}