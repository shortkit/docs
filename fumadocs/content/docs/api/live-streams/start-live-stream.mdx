---
title: "Start live stream"
description: "Mark a WebRTC live stream as `active` and publish it to your feed. Call this after your WebRTC publisher has connected and begun sending media \u2014 HLS streams go active automatically when the encoder connects, so they don't need this call. Idempotent: calling it on an already-active stream returns the current stream unchanged."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Mark a WebRTC live stream as `active` and publish it to your feed.
          Call this after your WebRTC publisher has connected and begun sending
          media — HLS streams go active automatically when the encoder connects,
          so they don't need this call. Idempotent: calling it on an
          already-active stream returns the current stream unchanged.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/live-streams/{stream_id}/start","method":"post"}]} />
    </>
  );
}