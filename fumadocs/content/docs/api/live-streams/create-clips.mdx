---
title: Create clips
description: >-
  Create one or more on-demand video clips from a live stream's recording — pass
  up to 20 ranges in `clips`, each with `startSec`, `endSec`, and an optional
  `title`. Works while the stream is `active` (real-time clipping) or after it
  has `ended`; returns 409 if the recording isn't available yet or the stream is
  in another state, and clipping is not supported for WebRTC streams. Each clip
  becomes a standalone video content item that starts in `processing`; supply a
  `callbackUrl` to be notified when each clip is ready.
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Create one or more on-demand video clips from a live stream's
          recording — pass up to 20 ranges in `clips`, each with `startSec`,
          `endSec`, and an optional `title`. Works while the stream is `active`
          (real-time clipping) or after it has `ended`; returns 409 if the
          recording isn't available yet or the stream is in another state, and
          clipping is not supported for WebRTC streams. Each clip becomes a
          standalone video content item that starts in `processing`; supply a
          `callbackUrl` to be notified when each clip is ready.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/live-streams/{stream_id}/clips","method":"post"}]} />
    </>
  );
}