---
title: "End live stream"
description: "End a live stream immediately, regardless of whether the broadcaster is still connected. The response returns the stream in its current state; for HLS streams the `status` transitions to `ended` asynchronously, so poll the stream or its status endpoint to confirm. Once ended, the recording becomes available for clipping."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          End a live stream immediately, regardless of whether the broadcaster
          is still connected. The response returns the stream in its current
          state; for HLS streams the `status` transitions to `ended`
          asynchronously, so poll the stream or its status endpoint to confirm.
          Once ended, the recording becomes available for clipping.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/live-streams/{stream_id}/end","method":"post"}]} />
    </>
  );
}