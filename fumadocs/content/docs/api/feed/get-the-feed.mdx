---
title: "Get the feed"
description: "Returns the paginated feed of published content for your organization \u2014 videos, image carousels, video carousels, and live streams, with any configured ad slots and surveys interleaved automatically. Use `limit` (1\u201350) and the `next_cursor` value from `meta` to paginate; pass the `X-User-Id` and `X-Session-Id` headers so survey frequency rules apply per user and session. Active live streams are ranked by current viewers and appear at the top of the first page."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Returns the paginated feed of published content for your organization
          — videos, image carousels, video carousels, and live streams, with any
          configured ad slots and surveys interleaved automatically. Use `limit`
          (1–50) and the `next_cursor` value from `meta` to paginate; pass the
          `X-User-Id` and `X-Session-Id` headers so survey frequency rules apply
          per user and session. Active live streams are ranked by current
          viewers and appear at the top of the first page.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/feed","method":"get"}]} />
    </>
  );
}