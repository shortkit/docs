---
title: "Get a filtered feed"
description: "Returns the feed narrowed by `tags`, `section`, `author`, `content_type`, or custom metadata via repeated `metadata.<key>=<value>` query parameters (multiple values for the same key match any of them). Pagination works the same as the main feed via `limit` and `cursor`. If discovery is enabled for your organization and at least one filter is present, results are served from the randomized discovery pipeline instead of reverse-chronological order."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Returns the feed narrowed by `tags`, `section`, `author`,
          `content_type`, or custom metadata via repeated
          `metadata.<key>=<value>` query parameters (multiple values for the
          same key match any of them). Pagination works the same as the main
          feed via `limit` and `cursor`. If discovery is enabled for your
          organization and at least one filter is present, results are served
          from the randomized discovery pipeline instead of
          reverse-chronological order.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/feed/filter","method":"get"}]} />
    </>
  );
}