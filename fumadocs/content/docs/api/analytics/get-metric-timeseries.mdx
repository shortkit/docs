---
title: "Get metric timeseries"
description: "Return time-bucketed values for a metric, suitable for charting. Granularity is automatic \u2014 hourly for ranges of 24 hours or less, daily otherwise \u2014 with empty buckets zero-filled and bucket boundaries aligned to your organization's timezone. Accepts the same `timeframe[]`, `filters[]`, and `measurement` parameters as the scalar metric endpoint."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Return time-bucketed values for a metric, suitable for charting.
          Granularity is automatic — hourly for ranges of 24 hours or less,
          daily otherwise — with empty buckets zero-filled and bucket boundaries
          aligned to your organization's timezone. Accepts the same
          `timeframe[]`, `filters[]`, and `measurement` parameters as the scalar
          metric endpoint.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/metrics/{metric}/timeseries","method":"get"}]} />
    </>
  );
}