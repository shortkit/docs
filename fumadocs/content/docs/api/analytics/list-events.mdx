---
title: List events
description: >-
  Return a paginated list of raw analytics events, newest first, including the
  event type, viewer, session identifiers, and the full event payload. Filter by
  `event_type`, `content_id`, or `viewer_id`. Set the time range with
  `timeframe[]`: relative as `value:unit` (for example `7:days` or `24:hours`)
  or absolute as two Unix timestamps (`timeframe[]=<start>&timeframe[]=<end>`);
  the default is `7:days`. `limit` accepts up to 200 events per page. See [Event
  types](/docs/api/guides/analytics-overview#event-types) for the full list of
  values the `event_type` filter accepts.
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Return a paginated list of raw analytics events, newest first,
          including the event type, viewer, session identifiers, and the full
          event payload. Filter by `event_type`, `content_id`, or `viewer_id`.
          Set the time range with `timeframe[]`: relative as `value:unit` (for
          example `7:days` or `24:hours`) or absolute as two Unix timestamps
          (`timeframe[]=<start>&timeframe[]=<end>`); the default is `7:days`.
          `limit` accepts up to 200 events per page. See [Event
          types](/docs/api/guides/analytics-overview#event-types) for the full list
          of values the `event_type` filter accepts.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/events/explore","method":"get"}]} />
    </>
  );
}