---
title: "Export views"
description: "Download raw view data for a timeframe as a file attachment. Set `format` to `json` (default) or `csv`; the standard `timeframe[]` and `filters[]` parameters apply. Exports are capped at 100,000 rows, newest first \u2014 when the cap is hit, the response includes an `X-Truncated: true` header."
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Download raw view data for a timeframe as a file attachment. Set
          `format` to `json` (default) or `csv`; the standard `timeframe[]` and
          `filters[]` parameters apply. Exports are capped at 100,000 rows,
          newest first — when the cap is hit, the response includes an
          `X-Truncated: true` header.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/exports/views","method":"get"}]} />
    </>
  );
}