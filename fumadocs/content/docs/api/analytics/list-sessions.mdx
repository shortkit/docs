---
title: List sessions
description: >-
  Return a paginated list of viewing sessions, each grouping its views (with
  their event timelines) plus any events not tied to a specific view. Each
  session includes its duration, view count, and event count. Supports the same
  `event_type`, `content_id`, `viewer_id`, and `count_only` parameters as the
  views endpoint.
full: true
_openapi:
  preload:
    - ./openapi/openapi.public.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Return a paginated list of viewing sessions, each grouping its views
          (with their event timelines) plus any events not tied to a specific
          view. Each session includes its duration, view count, and event count.
          Supports the same `event_type`, `content_id`, `viewer_id`, and
          `count_only` parameters as the views endpoint.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/openapi.public.json" operations={[{"path":"/v1/events/sessions","method":"get"}]} />
    </>
  );
}